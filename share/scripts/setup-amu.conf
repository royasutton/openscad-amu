###############################################################################
#
# Variable assignment notes:
#  - Do not quote variable names or values.
#  - Tokenize multi-value lists with ' ' (space-character).
#  - 'commands' options are specified as they are on the commnad line,
#    with multi-value lists Tokenized by ',' (comma-character).
#
# Supported variables
#  design_flow=df1
#  skip_check=no
#  skip_prep=no
#  apt_cyg_path=/usr/local/bin/apt-cyg
#  apt_get_opts=--verbose-versions --assume-yes
#  git_fetch_opts=--quiet
#  repo_url_apt_cyg=(can be local gitrepo cache)
#  repo_url=(can be local gitrepo cache)
#  repo_branch=develop
#  repo_branch_list=v1.7 v1.8.2 master develop
#  cache_install=no
#  repo_cache_root=cache
#  force_reconfigure=no
#  configure_opts_add=--with-boost=DIR --silent --prefix=${HOME}/openscad-amu
#  commands=--cache --branch master \
#           --install \
#           --template my_project
###############################################################################

#
# set defaults
#

#skip_check=yes
#apt_get_opts=--verbose-versions --assume-yes
#cache_install=yes
#repo_branch=develop


#
# example command: compiles release version v1.5.1 and the current master
# and development branches, installs to the cache directory, and creates
# project templates
#

#commands=\
#  --cache --branch v1.5.1 --install --template pt_v151 \
#  --reconfigure \
#  --cache --branch master --install --template pt_master \
#  --reconfigure \
#  --cache --branch develop --install --template pt_develop


# Similar result can be obtained using --branch-list option:

#commands=\
#  --branch-list v1.5.1,master,develop --cache --reconfigure \
#  --install --template-def


###############################################################################
# eof
###############################################################################
